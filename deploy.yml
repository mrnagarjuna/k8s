apiVersion: apps/v1
kind: Deployment
metadata:
  name: flaskappdeploy
  labels:
    app: flask_app  # This should match the selector and template labels
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask_app
  template:
    metadata:
      labels:
        app: flask_app
    spec:
      containers:
        - name: flaskappcontainer
          image: nagarjunaboddupalli/flask_demo
          ports:
            - containerPort: 8888